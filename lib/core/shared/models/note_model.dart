class NoteModel {
  final String title, content, createdAt;
  final int color;

  NoteModel({
    required this.title,
    required this.content,
    required this.createdAt,
    required this.color,
  });
}
